# 接口联调文档固定模板

生成 YApi 导入说明或飞书 Markdown 时，按以下顺序输出。字段没有源码、测试或 OpenAPI 证据时填写 `待确认`，不要删除该字段，也不要用推断替代。

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 需求名称 | `<需求名称>` |
| 生成目标 | `YApi` / `飞书` / `Postman` |
| 需求范围 | `<明确文件、模块或 Git 范围>` |
| 生成时间 | `<生成时间>` |
| 证据状态 | `已确认` / `源码确认` / `待确认` |

## 2. 联调前置条件

- 服务地址：`<环境变量或待确认>`
- 认证方式：`<源码/安全配置确认；无证据则待确认>`
- 必填公共 Header：`<Header、来源、是否敏感>`
- 数据准备：`<仅记录有证据的前置数据>`
- 统一错误格式：`<异常处理器或待确认>`

## 3. 接口清单

| 编号 | 接口名称 | 方法 | 路径 | 认证 | 状态 |
|---|---|---|---|---|---|
| 1 | `<summary>` | `GET` | `/example` | `<认证>` | `已确认` |

## 4. 接口详情

每个接口都使用下面的小节，不因字段为空而省略。

### 4.1 `<接口名称>`

#### 基本信息

| 项目 | 内容 |
|---|---|
| 方法 | `<HTTP method>` |
| 路径 | `<path>` |
| 用途 | `<summary/description>` |
| 认证 | `<security 配置或待确认>` |
| 成功状态码 | `<responses 中的状态码>` |
| 证据 | `<文件路径、符号、测试或 OpenAPI>` |

#### 请求参数

| 位置 | 字段 | 类型 | 必填 | 可空 | 约束 | 示例 | 说明 | 证据状态 |
|---|---|---|---|---|---|---|---|---|
| path/query/header/body | `<name>` | `<type>` | `<yes/no/待确认>` | `<yes/no/待确认>` | `<constraint>` | `<example>` | `<description>` | `<状态>` |

#### 请求示例

```http
<method> <path> HTTP/1.1
Host: <host>
<header>: <value>

<body>
```

#### 成功响应

```json
<只填有证据的响应示例；没有示例则保留最小结构并标记待确认>
```

| 字段 | 类型 | 必返 | 可空 | 说明 | 证据状态 |
|---|---|---|---|---|---|
| `<field>` | `<type>` | `<yes/no/待确认>` | `<yes/no/待确认>` | `<description>` | `<状态>` |

#### 失败响应

| HTTP 状态码 | 错误码 | 触发条件 | 消息 | 证据状态 |
|---|---|---|---|---|
| `<status>` | `<code/待确认>` | `<condition>` | `<message/待确认>` | `<状态>` |

#### 联调示例

```bash
curl --request <METHOD> '<baseUrl><path>' \
  --header '<header>: <placeholder>'
```

敏感 Header、Token、Cookie 只能使用变量名或脱敏占位符，不能填真实值。

## 5. 待确认项

- `<无法从源码、OpenAPI、测试或用户要求确认的内容>`

## 6. 验证记录

- 静态取证：`<范围和文件>`
- OpenAPI 校验：`通过` / `未执行` / `<失败原因>`
- Postman/Newman：`未生成` / `Collection 已生成，未执行` / `<执行结果>`
- YApi 远程写入：`未执行` / `dry-run` / `<用户明确执行后的结果>`
- 飞书远程写入：`未执行` / `dry-run` / `<用户明确执行后的结果>`
